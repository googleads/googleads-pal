#import <UIKit/UIKit.h>

/**
 * A simple view controller that displays a navigation bar with 2 buttons to:
 *   1. Request, load, and display a nonce (or error) with the PAL SDK.
 *   2. Reset the nonce loader being used.
 */
@interface ViewController : UIViewController
@end
